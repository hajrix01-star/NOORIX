# أضف داخل server { ... } لموقع hajrix.com حتى يعمل SPA تحت /tax/
# ثم: sudo nginx -t && sudo systemctl reload nginx

location /tax/ {
    alias /var/www/hajrix.com/tax/;
    try_files $uri $uri/ /tax/index.html;
}

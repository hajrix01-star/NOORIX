{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "buildCommand": "cd backend && npm install && npx prisma generate && npm run build"
  },
  "deploy": {
    "startCommand": "cd backend && npm run start:prod:full",
    "healthcheckPath": null
  }
}

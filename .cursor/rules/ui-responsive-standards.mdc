---
description: Noorix — معايير الاستجابة والكثافة بـ Tailwind CSS v4 (سطح المكتب + الجوال)
globs: src/**/*.{jsx,tsx}
alwaysApply: true
---

# Noorix — معايير الاستجابة والكثافة (Tailwind v4)

## نقاط الكسر الرسمية (Breakpoints)

| الاسم | العرض | Tailwind | الاستخدام |
|----|-----|---|---|
| **mobile** | `max-width: 640px` | افتراضي (بدون prefix) | هاتف |
| **sm** | `641px+` | `sm:` | تابلت صغير |
| **lg** | `1024px+` | `lg:` | سطح المكتب |

```jsx
// ✅ عمودي على الجوال، صف على سطح المكتب
<div className="flex flex-col sm:flex-row gap-3">

// ✅ شبكة متجاوبة
<div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">

// ✅ حشو داخلي لبطاقة أو عمود (ليس جذر الشاشة داخل app-main)
<div className="p-4 lg:p-6">
```

---

## جذر شاشات الأقسام (داخل `app-main`) — إلزامي

تحت العرض الضيق يوفّر `.app-main` هامشاً أفقياً (`clamp` + `app-main__content`). إذا أضاف جذر الشاشة `px-3` على الجوال يتضاعف الهامش ويضيق المحتوى.

**القانون الموحّد (طبقة واحدة — لا تنسخ الـ classes يدوياً في شاشات جديدة):**

```jsx
import { ScreenShell } from '../../ui';

<ScreenShell>
  …
</ScreenShell>
```

يُطبَّق داخلياً نفس السلوك التالي (ثابت `SCREEN_SHELL_PAGE_CLASS` في `src/ui/ScreenShell.jsx`):

| العرض | السلوك |
|----|-----|
| أقل من `md` (768px) | `px-0` — الاعتماد على حشو `app-main` فقط |
| `md` فما فوق | `px-3` أفقياً |
| `lg` | `px-6` أفقياً |
| دائماً | `py-4` رأسياً + `gap-4` بين أقسام الصفحة |

**مضمّن داخل تبويب/كرت أب** (مثل قائمة الموظفين داخل HR): `<ScreenShell embedded>` — بدون `py`/`px` على الجذر حتى لا يتضاعف الهامش مع `ScreenShell` الخارجي.

استخدم **`md:`** (وليس `sm:`) ليتماشى مع استعلام التطبيق لـ `.app-main`. لا تستخدم `p-4 lg:p-6` على جذر الشاشة الجديدة إلا لسبب موثّق (مثلاً شاشة خارج الإطار العام).

---

## أزرار `Button` — إلزامي حسب السياق

| السياق | الـ `size` |
|-----|---|
| شريط أدوات، رأس صفحة، صف فلاتر | **`sm`** |
| خلايا جدول / إجراءات صف | **`sm`** |
| زر إرسال رئيسي في مودال | **`md`** (افتراضي) |
| زر بارز وحيد (تسجيل دخول) | **`lg`** |

---

## بطاقات (Cards)

```jsx
// ✅ بطاقة قياسية
<div className="bg-noorix-surface border border-noorix-border rounded-xl shadow-sm p-4">

// ✅ بطاقة من المكوّن
import { Card } from '../../ui';
<Card>محتوى</Card>

// ✅ شبكة بطاقات KPI متجاوبة
<div className="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 gap-3">
```

---

## هيكل الشاشة القياسي

```jsx
// ✅ شاشة كاملة مع رأس وجدول
import { ScreenShell, Button, SmartTable } from '../../ui';

<ScreenShell>
  <div className="flex flex-wrap items-center justify-between gap-3">
    <h1 className="text-[20px] font-bold text-noorix-text m-0">{t('title')}</h1>
    <div className="flex items-center gap-2 flex-wrap">
      <Button size="sm" variant="primary">{t('add')}</Button>
    </div>
  </div>
  <SmartTable columns={cols} data={rows} … />
</ScreenShell>
```

---

## بطاقات الجوال في SmartTable

```jsx
// ✅ renderMobileCard مع Tailwind
renderMobileCard={(row) => (
  <div className="flex flex-col gap-2">
    <div className="flex items-center justify-between gap-2">
      <span className="font-bold text-noorix-blue ltr">#{row.number}</span>
      <Badge {...Badge.fromStatus(row.status, STATUS_MAP)} size="sm" />
    </div>
    <div className="grid grid-cols-3 gap-2">
      <div>
        <div className="text-[11px] text-noorix-muted">{t('total')}</div>
        <div className="text-[13px] font-bold text-noorix-green ltr">{fmt(row.total, 2)}</div>
      </div>
    </div>
  </div>
)}
```

---

## فحص يدوي سريع (3 عروض)

1280px → 768px → 375px — مسار: لوحة → قسم يومي → جدول → مودال

**قاعدة الجوال:**
- لا عرض ثابت بـ style={{ width: 'NNNpx' }} على العناصر الظاهرة
- الجداول داخل `overflow-x-auto` دائماً
- أزرار مهمة ≥ `min-h-[44px]` على مسارات حرجة

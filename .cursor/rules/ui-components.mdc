---
description: STRICT — Enforce Noorix UI system (src/ui). Block raw HTML, inline styles for layout/colors, and old noorix-btn-* classes. See ui-responsive-standards.mdc for breakpoints, Button size, mobile cards.
globs: src/**/*.{jsx,tsx,js,ts}
alwaysApply: true
---

# Noorix UI System — قواعد صارمة (استثناءات محددة وموثّقة)

## 🚨 المحظورات المطلقة (سبب رفض الكود)

### 1. العناصر الخام (Raw HTML) — ممنوع بالكامل

```jsx
// ❌ يُرفض فوراً
<button ...>
<input type="text|number|date|search|email|tel|url|password|month|time" ...>
<select ...>

// ✅ البديل الوحيد المقبول
import { Button, Input } from '../../ui';
<Button variant="primary|danger|success|ghost|warning|raw">
<Input type="text|number|date|select|search|...">
```

### 2. الـ Inline Styles للتخطيط والألوان — محظور تماماً

```jsx
// ❌ يُرفض — تخطيط ثابت
<div style={{ display: 'flex', alignItems: 'center', gap: 12 }}>
<div style={{ padding: 16, borderRadius: 8 }}>
<div style={{ fontSize: 13, fontWeight: 600 }}>
<div style={{ color: 'var(--noorix-text-muted)' }}>
<div style={{ background: 'var(--noorix-bg-muted)' }}>
<div style={{ width: '100%', overflow: 'auto' }}>

// ✅ البديل — استخدم classes من src/ui/ui.css
<div className="nx-flex-center nx-gap-12">
<div className="nx-p-16 nx-rounded">
<div className="nx-text-base nx-font-600">
<div className="nx-text-muted">
<div className="nx-bg-muted">
<div className="nx-w-full nx-overflow-auto">

// ✅ مقبول — قيم ديناميكية محسوبة من state/props (انظر أيضاً «طبقة الوظائف» أدناه)
style={{ width: `${progress}%` }}
style={{ gridTemplateColumns: `repeat(${cols}, 1fr)` }}
style={{ height: rowHeight * count }}
style={{ color: item.dynamicColor }}     // من data
style={{ top: menuY, left: menuX }}      // من DOM measurements
```

**طبقة الوظائف (Functional inline — ليست قرارات تصميم بصرية):** مسموحة عندما تخدم سلوك المكوّن (مودال، قائمة منسدلة، منطقة تمرير)، مع تفضيل إضافة class في `ui.css` إذا تكرّرت القيمة.

- `zIndex` — طبقات فوق بعض (مع احترام `--nx-z-*` إن وُجدت)
- `minWidth` / `maxWidth` / `minHeight` / `maxHeight` — حدود تخطيط للمكوّن نفسه (مثلاً عرض أدنى للقائمة، `maxHeight` لجسم المودال)
- `transform` — حركة/موضع محسوب (انتقالات UI)
- `transitionDuration` — عندما تكون القيمة ديناميكية من state

**ممنوع** استخدام هذه الخصائص كبديل عن تخطيط الصفحة العام (صفوف، شبكات، هوامش) — هناك تبقى `nx-*` فقط.

### 3. الكلاسات القديمة — محظور ذكرها

```jsx
// ❌ dead code — تمت إزالتها من index.css
className="noorix-btn-nav"
className="noorix-btn-primary"
className="noorix-btn-success"
className="noorix-btn-danger"
className="noorix-btn-ghost"
className="noorix-btn"
className="noorix-btn--primary"
```

### 4. الجداول — SmartTable إلزامي لشاشات البيانات

```jsx
// ❌ جداول HTML في شاشات بيانات: ترقيم، فرز، بحث، تحميل دفعات
<table><thead><tr><th>...</th></tr></thead><tbody>...

// ✅ استخدم SmartTable
import { SmartTable } from '../../ui';
<SmartTable columns={cols} data={rows} total={total} page={page} onPageChange={setPage} />

// ✅ استثناء: جدول HTML بسيط ثابت داخل كرت / ملخص (≤ 10 صفوف بيانات)
//    أمثلة: تفصيل ضريبة، إجمالي فاتورة، مقارنة KPI قصيرة — بدون ترقيم/فرز خادم
// ✅ استثناء: مستندات الطباعة (PDF/print)
```

### 5. النوافذ المنبثقة والألواح — ممنوع portal/overlay يدوي

```jsx
// ❌
createPortal(<div style={{ position:'fixed', inset:0, background:'rgba(0,0,0,0.5)' }}> ... </div>)
<div className="modal-overlay"> ... </div>

// ✅ نوافذ حوارية مركزية
import { Modal } from '../../ui';
<Modal open={open} onClose={onClose} title="العنوان" size="md|lg|sm">
  {children}
</Modal>
```

**الألواح الجانبية (drawer / side panel):** ليست بديلاً عن `Modal` المركزي. حالياً لا يوجد `Drawer` موحّد في `src/ui` — **ممنوع** بناء overlay جانبي مخصّص بـ portal؛ عند الحاجة (تفاصيل سجل، معاينة فاتورة، سجل نشاط) يُضاف مكوّن **`Drawer`** (أو امتداد رسمي) في `src/ui` بنفس منظومة z-index وتركيز لوحة المفاتيح وARIA مثل `Modal`.

### 6. قواعد استخدام المكوّنات (Semantic usage)

الفوضى لا تأتي من CSS فقط — **معنى** الزر/الشارة يجب أن يكون متوقعاً عبر التطبيق.

| المكوّن | الاستخدام المقبول |
|---------|-------------------|
| **Button `primary`** | إجراء رئيسي واحد **للوحة/القسم** (CTA واحد واضح) — ليس كل زر في الشريط |
| **Button `danger`** | إجراءات تدميرية فقط (حذف، إلغاء نهائي، تفريغ) — مع تأكيد عند الخطر |
| **Button `success`** | إتمام نجاح (حفظ، اعتماد، إغلاق دورة) — بحسب سياق الشاشة |
| **Button `ghost`** | إجراءات ثانوية (إلغاء، رجوع، «المزيد» غير الحاسم) |
| **Button `warning`** | تحذير لكن ليس بالضرورة تدمير (تعليق، إيقاف مؤقت) |
| **Badge** | حالات وحالات فقط (حالة طلب، نوع، عدّاد مقروء) — ليس عنواناً بديلاً لنص عادي |
| **Card** | تجميع محتوى مترابط — لا تكديس كرت داخل كرت بدون حاجة |

عند الشك: **primary أقل = أوضح** للمستخدم.

---

## ✅ مرجع الـ Utility Classes (src/ui/ui.css)

### Layout
| Class | CSS |
|-------|-----|
| `nx-flex` | `display: flex` |
| `nx-flex-center` | `display:flex; align-items:center` |
| `nx-flex-between` | `display:flex; align-items:center; justify-content:space-between` |
| `nx-flex-col` | `display:flex; flex-direction:column` |
| `nx-flex-end` | `display:flex; align-items:center; justify-content:flex-end` |
| `nx-flex-wrap` | `display:flex; flex-wrap:wrap` |
| `nx-grid` | `display:grid` |
| `nx-grid-2` | `display:grid; grid-template-columns:1fr 1fr` |
| `nx-grid-3` | `display:grid; grid-template-columns:1fr 1fr 1fr` |
| `nx-flex-1` | `flex:1; min-width:0` |
| `nx-row-between` | flex + space-between + flex-wrap |

### Gap
`nx-gap-4` `nx-gap-6` `nx-gap-8` `nx-gap-10` `nx-gap-12` `nx-gap-14` `nx-gap-16` `nx-gap-20` `nx-gap-24`

### Typography
| Class | Value |
|-------|-------|
| `nx-text-xs` | font-size: 11px |
| `nx-text-sm` | font-size: 12px |
| `nx-text-base` | font-size: 13px |
| `nx-text-md` | font-size: 14px |
| `nx-text-lg` | font-size: 15px |
| `nx-text-xl` | font-size: 16px |
| `nx-text-2xl` | font-size: 18px |
| `nx-text-3xl` | font-size: 20px |
| `nx-font-500/600/700/800` | font-weight |
| `nx-text-muted` | color: var(--noorix-text-muted) |
| `nx-text-primary` | color: var(--noorix-text) |
| `nx-text-center/end/start` | text-align |
| `nx-truncate` | ellipsis overflow |
| `nx-ltr` | direction: ltr |
| `nx-uppercase` | text-transform: uppercase |

### Spacing
`nx-p-4/6/8/10/12/14/16/20/24`
`nx-px-8/12/16` | `nx-py-4/6/8/12`
`nx-m-0` | `nx-mt-0/4/6/8/10/12/16/20` | `nx-mb-0/4/6/8/12/16`

### Surface
| Class | CSS |
|-------|-----|
| `nx-surface` | bg-surface + border + border-radius 12px |
| `nx-surface-sm` | bg-surface + border + border-radius 8px |
| `nx-bg-muted` | background: var(--noorix-bg-muted) |
| `nx-bg-surface` | background: var(--noorix-bg-surface) |
| `nx-border-all` | border: 1px solid var(--noorix-border) |
| `nx-border-b/t` | border-bottom/top |
| `nx-rounded` | border-radius: 8px |
| `nx-rounded-lg` | border-radius: 12px |

### Misc
`nx-w-full` | `nx-overflow-auto` | `nx-overflow-hidden` | `nx-cursor-pointer`
`nx-ltr` | `nx-rtl` | `nx-select-none` | `nx-truncate` | `nx-nowrap`

---

## الاستيراد الصحيح

```jsx
import { Button, Input, Card, Badge, Modal, Spinner, SmartTable, Divider, FormRow } from '../../ui';
// أو حسب عمق الملف:
import { Button, Input, Modal } from '../../../ui';
```

---

## متى يُسمح بـ inline style؟ (ملخّص)

1. **قيمة مئوية محسوبة**: `style={{ width: `${pct}%` }}`
2. **أبعاد من state**: `style={{ height: chartHeight, width: chartWidth }}`
3. **ألوان من البيانات**: `style={{ color: category.color }}`
4. **إحداثيات من DOM**: `style={{ top: y, left: x }}` (قوائم منسدلة محسوبة)
5. **CSS Grid الديناميكي**: `style={{ gridTemplateColumns: `repeat(${n}, 1fr)` }}`
6. **طبقة الوظائف** (انظر §2): `zIndex`, `min/max` أبعاد للمكوّن، `transform`, `transitionDuration` ديناميكي
7. **مستندات الطباعة** (print/PDF): أنماط دقيقة للطباعة

**تخطيط الصفحة والألوان الثابتة عبر `style` = مرفوض.** ما عدا القائمة أعلاه.

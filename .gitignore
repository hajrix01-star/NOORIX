# Environment — لا ترفع أبداً (يحتوي مفاتيح سرية)
.env
.env.local
.env.*.local
backend/.env

# Dependencies
node_modules/

# Build
dist/
build/
build-out.txt
cursorrules*.txt

# Runtime uploads (OCR, HR, attachments) — do not track
backend/uploads/

# Logs
*.log

# IDE / OS — قواعد Cursor المشتركة تُرفع تحت .cursor/rules/
.cursor/*
!.cursor/rules/
!.cursor/rules/**/*.mdc
.idea/
.vscode/
.DS_Store

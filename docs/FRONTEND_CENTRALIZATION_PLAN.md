# توحيد الواجهة والهوكات — **الخطة**

**حالة التقسيم:** ✅ وُضع هذا القسم في ملف مستقل (2026-04-08).

**روابط:** [الأوديت](./FRONTEND_CENTRALIZATION_AUDIT.md) · [التشيك ليست](./FRONTEND_CENTRALIZATION_CHECKLIST.md) · [الفهرس](./FRONTEND_CENTRALIZATION_AUDIT_PLAN_CHECKLIST.md)

---

**مبدأ:** PRs صغيرة، قابلة للمراجعة، قابلة للرجوع.

| المرحلة | الهدف | مخرجات تقريبية |
|---------|--------|----------------|
| **0 — تثبيت** | عدم توسيع الدين | عدم إدخال تبويبات/خرائط جديدة يدوياً في شاشات جديدة؛ الالتزام بـ `ScreenTabs` والقواعد الحالية. |
| **1 — تخزين ولغة** | إزالة ازدواجية مفتاح اللغة + مساعدات JSON | `storageKeys.js` + `readJsonStorage` / `writeJsonStorage`؛ تحديث `App.jsx` + `main.jsx` + هجرة قراءة المفتاح القديم. |
| **2 — هوكات عرض عامة** | `useMediaQuery` + `useDebouncedValue` | استبدال النسخ المكررة في الشاشات المذكورة في الأوديت؛ مراجعة `UserMenu` (اشتراك أو hook). |
| **3 — هيكل شاشة** | اتساق الهامش والعنوان | مكوّن رقيق `ScreenShell` + `ScreenTitle` (أو تبني على موجود) + إصلاح الشاشات ذات `p-4 lg:p-6` على الجذر. |
| **4 — تبويبات** | هجرة الأشرطة اليدوية | توسيع `ScreenTabs` إن لزم (مثلاً لاحقة تبويب)؛ هجرة Settings / Orders / OCR / Bank analysis. |
| **5 — كباب موحّد** | `KebabMenu` أو `TableRowActionsMenu` في `src/ui` | دمج الخلايا الثلاث؛ اختبار لوحة مفاتيح ونقر خارج. |
| **6 — شارات وثوابت** | `badgeMaps` أو بنّاءات لكل مجال | فواتير/مبيعات/مشتريات؛ ثم مصروفات؛ ثم HR تدريجياً. |
| **7 — بيانات** | React Query | توسيع `useVaults` للنطاق الزمني إن لزم؛ مواءمة مفاتيح الموظفين/الإقامات؛ نقل `useBankStatementView` إن وافقت السياسة. |
| **8 — جداول كبيرة** | `OrdersTab` → `SmartTable` | مشروع فرعي مع اختبار يدوي كامل للفلاتر والطباعة. |

**الترتيب الموصى به للبدء:** 1 → 2 → 3 → 4 → 5، ثم 6 و 7 حسب الصدمة التجارية، ثم 8 عند توفر وقت اختبار.

**تتبع التنفيذ:** استخدم [التشيك ليست](./FRONTEND_CENTRALIZATION_CHECKLIST.md) وضع `[x]` على البنود عند إنجازها في الكود.

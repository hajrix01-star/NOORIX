# مطابقة خطة التنفيذ 17/17 — فجوات مقابل الكود (2026-04-27)

المرجع: [NOORIX_EXECUTION_PLAN.md](./NOORIX_EXECUTION_PLAN.md). الحالة: **مُستوفى جزئياً أو كاملاً** حسب البند؛ لا يعني «إغلاق المنتج» بل **مطابقة تصميمية** مع ما هو منجز حالياً.

| # | البند | الحالة في المستودع | ملاحظة |
|---|--------|-------------------|---------|
| 1 | PostgreSQL + فهارس 100k+ | **مستوفى** | Prisma + فهارس على `LedgerEntry` وغيره؛ مراقبة `EXPLAIN` عند البطء. |
| 2 | Decimal.js للمالية | **مستوفى** | Backend + Frontend dependencies؛ يُراجع عند مسارات حساب جديدة. |
| 3 | FK منع حذف مورد مرتبط | **مستوفى** | `schema.prisma` — سلوك الحذف/الأرشفة في الخدمات. |
| 4 | توقيت السعودية UTC+3 | **مستوفى** | `date-utils`، `toYmd`؛ تمييز حقول الرياض في الواجهة. |
| 5 | هوية بصرية 260px | **مُنقح** | `index.css` يستخدم `clamp` وشبكات بعرض 260px — ليس بالضرورة `margin-right: 260px` حرفياً على `.app-main`. |
| 6 | قيد مزدوج تلقائي | **مستوفى** | `financial-core` + `LedgerEntry`. |
| 7 | عزل `company_id` | **مستوفى** | Guards + `TenantPrismaService` + `x-company-id`. |
| 8 | Pagination من الخادم | **جزئي** | فواتير/قوائم رئيسية مدعومة؛ يُراجع أي شاشة جديدة بدون ترقيم. |
| 9 | ACID / `$transaction` | **مستوفى** | أنماط شائعة في الخدمات المالية. |
| 10 | فرادة أرقام فواتير | **مستوفى** | قيود `schema` + توليد سيريال. |
| 11 | React Query للكاش | **مستوفى** | + `invalidateOnFinancialMutation`. |
| 12 | SmartTable RTL | **مستوفى** | `src/ui` + هجرات موثّقة في checklist الواجهة. |
| 13 | Error Boundary | **مستوفى** | `main.tsx` يلفّ التطبيق بـ `ErrorBoundary`. |
| 14 | FTS سريع في PostgreSQL | **جزئي / لاحق** | فهارس؛ بحث full-text عام غير مؤكد على كل الجداول. |
| 15 | Audit Log | **مستوفى (نموذج)** | `AuditLog` في `schema.prisma` — التغطية الوظيفية لكل العمليات تُراجع حسب المنتج. |
| 16 | تحقق من البيانات في الخادم | **مستوفى** | `class-validator` + `ValidationPipe`؛ ليس Zod على كل المسارات. |
| 17 | Session timeout | **جزئي** | JWT `JWT_EXPIRES_IN` + `sessionStorage` للتوكن؛ **لا** انتهاء خمول منفصل عن انتهاء JWT (موثّق في `AuthContext`). |

**الخلاصة:** البنود الحرجة للنواة المالية والعزل والكاش **مغطاة**. الفجوات الواضحة للتوسع: **FTS شامل**، **pagination لكل الشاشات الجديدة**، **idle timeout منفصل عن JWT** إن اشترطه المنتج.
